---
title: Korean font configuration in matplotlib
date: 2025-10-18
time: 01:29
uuid: 31f7ea84-1969-4500-acbb-1a40b835e16a
type: conversation
tags: [archive, conversation]
---

# Korean font configuration in matplotlib

**날짜**: 2025-10-18 01:29

---

## Human (Message 1)

구문오류plt.rc(font, family='Malgun Gothic')

```python

```

## Claude (Message 2)

이 구문 오류는 `font`를 문자열로 감싸지 않아서 발생합니다. 올바른 코드는 다음과 같습니다:

```python
plt.rc('font', family='Malgun Gothic')
```

**전체 예시:**

```python
import matplotlib.pyplot as plt

# 한글 폰트 설정
plt.rc('font', family='Malgun Gothic')

# 마이너스 기호 깨짐 방지
plt.rc('axes', unicode_minus=False)

# 이제 한글이 정상적으로 표시됩니다
plt.plot([1, 2, 3], [1, 2, 3])
plt.title('한글 제목')
plt.xlabel('x축')
plt.ylabel('y축')
plt.show()
```

**주요 차이점:**

- ❌ `plt.rc(font, family='Malgun Gothic')` - font가 변수로 인식됨
- ✅ `plt.rc('font', family='Malgun Gothic')` - 'font'를 문자열로 전달

**다른 한글 폰트 옵션:**

- Windows: `'Malgun Gothic'`, `'굴림'`, `'돋움'`
- Mac: `'AppleGothic'`
- Linux: `'NanumGothic'`

---

_Archived from: `31f7ea84-1969-4500-acbb-1a40b835e16a.json`_
