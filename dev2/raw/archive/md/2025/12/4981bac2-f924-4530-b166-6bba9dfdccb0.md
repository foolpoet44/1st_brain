---
title: Udemy API course information search
date: 2025-12-10
time: 06:04
uuid: 4981bac2-f924-4530-b166-6bba9dfdccb0
type: conversation
tags: [archive, conversation]
---

# Udemy API course information search

**날짜**: 2025-12-10 06:04

---

## Human (Message 1)

유데미 API를 활용한 과정정보 검색

## Claude (Message 2)

사용자가 "유데미 API를 활용한 과정정보 검색"이라고 한국어로 요청했습니다. 이것은 Udemy API를 사용하여 강좌 정보를 검색하는 것에 대한 질문으로 보입니다.

사용자가 원하는 것이 무엇인지 명확하지 않습니다:

1. Udemy API 사용 방법에 대한 설명을 원하는가?
2. 실제로 Udemy API를 호출하여 강좌를 검색하고 싶은가?
3. Udemy API를 활용한 프로젝트/코드를 만들고 싶은가?

저는 먼저 사용자의 의도를 명확히 파악해야 합니다. 그리고 실제로 Udemy API를 호출할 수 있는 도구는 현재 제공되지 않으므로, web_search를 통해 최신 정보를 찾거나, 코드 예제를 작성하는 방법으로 도움을 줄 수 있습니다.

한국어로 응답해야 합니다.안녕하세요! Udemy API를 활용한 강좌 정보 검색에 대해 도움을 드리겠습니다.

구체적으로 어떤 도움이 필요하신지 알려주시겠어요?

1. **Udemy API 사용 방법** - API 연동 방법이나 인증 절차를 알고 싶으신가요?

2. **코드 예제** - Python, JavaScript 등 특정 언어로 API를 호출하는 코드를 만들고 싶으신가요?

3. **특정 강좌 검색** - 특정 주제나 키워드로 Udemy 강좌를 찾아보고 싶으신가요?

4. **프로젝트 개발** - Udemy API를 활용한 애플리케이션을 만들고 싶으신가요?

원하시는 내용을 좀 더 구체적으로 말씀해 주시면, 그에 맞는 정보나 코드를 제공해 드리겠습니다!

## Human (Message 3)

1

## Claude (Message 4)

사용자가 Udemy API 사용 방법에 대해 알고 싶어합니다. Udemy API의 연동 방법, 인증 절차, 기본적인 사용법 등을 설명해드리겠습니다.

먼저 최신 정보를 확인하기 위해 웹 검색을 해야 할 것 같습니다. Udemy API는 제 지식 컷오프 이후에 변경사항이 있을 수 있으므로, 현재의 정확한 정보를 제공하기 위해 검색하는 것이 좋겠습니다.Udemy API의 최신 정보를 확인해드리겠습니다.

```
This block is not supported on your current device yet.
```

좋습니다. Udemy API에 대한 정보를 검색했습니다. 검색 결과를 보면 두 가지 주요 API가 있는 것 같습니다:

1. **Udemy Instructor API** - 강사들을 위한 API (maintenance mode)
2. **Udemy for Business API** - 기업 고객용 API (활발히 사용 중)

사용자가 어떤 API를 원하는지 모르지만, 일반적인 강좌 검색을 원한다면 Udemy for Business API가 더 적합할 것 같습니다. 하지만 개인 사용자가 일반 공개 API를 찾고 있다면, Udemy의 공개 API가 제한적이거나 현재 유료/승인 기반일 수 있습니다.

주요 정보를 정리하겠습니다:

1. API 인증 방법
2. API 엔드포인트
3. 사용 가능한 기능
4. 제한사항

사용자에게 명확하고 실용적인 정보를 제공하겠습니다.# Udemy API 사용 방법

Udemy는 크게 **두 가지 종류의 API**를 제공합니다:

## 1. **Udemy Instructor API** (강사용)

현재 유지보수 모드이며 새 기능 추가 없음

## 2. **Udemy for Business API** (기업용)

활발히 사용되고 있으며, 강좌 검색 및 데이터 조회에 적합

---

## 🔑 API 인증 절차

### **Step 1: API 클라이언트 생성**

**개인 강사 계정의 경우:**

1. Udemy.com에 로그인
2. 프로필 → **API Clients** 페이지 이동
3. API 클라이언트 요청
4. 승인 후 **Bearer Token** 발급

**Udemy for Business의 경우:**

1. Udemy Business 관리자 계정으로 로그인
2. **Manage** → **Settings** → **APIs & integrations**
3. 통합할 시스템 선택
4. 다음 자격 증명 획득:
   - Client ID
   - Client Secret
   - Organization ID
   - Organization Name (Portal Name)

### **Step 2: Bearer Token 생성**

Client ID와 Client Secret를 Base64로 인코딩하여 Authorization 헤더 생성:

```python
import base64

client_id = "your_client_id"
client_secret = "your_client_secret"

# Base64 인코딩
credentials = f"{client_id}:{client_secret}"
encoded_credentials = base64.b64encode(credentials.encode()).decode()
bearer_token = f"Basic {encoded_credentials}"
```

---

## 📡 API 호출 예제

### **Python 예제 - 강좌 목록 조회**

```python
import requests
import base64

# 인증 정보
client_id = "YOUR_CLIENT_ID"
client_secret = "YOUR_CLIENT_SECRET"
organization_id = "YOUR_ORG_ID"  # Business API용

# Bearer Token 생성
credentials = f"{client_id}:{client_secret}"
encoded = base64.b64encode(credentials.encode()).decode()

# API 호출
url = f"https://YOUR_PORTAL.udemy.com/api-2.0/organizations/{organization_id}/courses/list/"

headers = {
    "Authorization": f"Basic {encoded}",
    "Content-Type": "application/json"
}

params = {
    "fields[courses]": "@all",
    "page": 1,
    "page_size": 10
}

response = requests.get(url, headers=headers, params=params)
courses = response.json()

print(courses)
```

### **JavaScript (Node.js) 예제**

```javascript
const axios = require("axios");

const clientId = "YOUR_CLIENT_ID";
const clientSecret = "YOUR_CLIENT_SECRET";
const organizationId = "YOUR_ORG_ID";
const portalName = "YOUR_PORTAL_NAME";

// Bearer Token 생성
const token = Buffer.from(`${clientId}:${clientSecret}`).toString("base64");

// API 호출
const url = `https://${portalName}.udemy.com/api-2.0/organizations/${organizationId}/courses/list/`;

axios
  .get(url, {
    headers: {
      Authorization: `Basic ${token}`,
      "Content-Type": "application/json",
    },
    params: {
      "fields[courses]": "@all",
      page: 1,
      page_size: 10,
    },
  })
  .then((response) => {
    console.log(response.data);
  })
  .catch((error) => {
    console.error(error);
  });
```

---

## 📋 주요 엔드포인트

| 엔드포인트                                      | 설명                |
| ----------------------------------------------- | ------------------- |
| `/api-2.0/organizations/{org_id}/courses/list/` | 강좌 목록 조회      |
| `/api-2.0/courses/{course_id}/`                 | 특정 강좌 상세 정보 |
| `/api-2.0/organizations/{org_id}/users/`        | 사용자 정보 조회    |
| `/api-2.0/organizations/{org_id}/analytics/`    | 학습 분석 데이터    |

---

## ⚠️ 중요 제한사항

1. **Rate Limiting**: 100 요청/10초
2. **HTTPS 전용**: HTTP는 지원하지 않음
3. **인증 필수**: 모든 요청에 Bearer Token 포함 필요
4. **접근 권한**:
   - Instructor API: 개인 계정 승인 필요
   - Business API: Enterprise 고객만 이용 가능

---

## 💡 추가 팁

- **GraphQL API**: 최신 기능은 GraphQL API로 제공됨 (Business 고객용)
- **xAPI**: 실시간 학습 데이터 추적 웹훅 제공
- **SCIM**: 사용자 프로비저닝 자동화 가능

---

구체적으로 어떤 프로젝트를 진행하시나요? 더 상세한 코드나 특정 기능 구현을 도와드리겠습니다!

---

_Archived from: `4981bac2-f924-4530-b166-6bba9dfdccb0.json`_
