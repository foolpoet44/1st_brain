---
title: Claude 코드 이슈 해결
date: 2026-02-12
time: 02:01
uuid: 542c1e6f-745c-47b4-ac42-2638df92522c
type: conversation
tags: [archive, conversation]
---

# Claude 코드 이슈 해결

**날짜**: 2026-02-12 02:01

---

## Human (Message 1)

클로드 코드에서 발생한 다음 이슈 해결안 제공

## Claude (Message 2)

첨부하신 문서를 보니 Claude Code에서 4.x 모델 접근 오류가 발생하고 있는 상황이군요.

그런데 솔직히 말씀드리면, 이 문서의 핵심 전제가 현재 시점에서 **정확하지 않습니다.**

---

## 실제 상황 진단

문서에서 "4.x 시리즈는 Claude Code에서 지원되지 않는다"고 적혀 있지만, 이건 제가 최근 방향을 파악할 때 잘못된 정보입니다. `claude-sonnet-4-5-20250929`는 현재 기준으로 **정식 지원 모델**이고, Claude Code의 기본 모델이기도 합니다.

실제 오류 원인은 모델 자체의 문제가 아니라 다른 곳에 있을 가능성이 높습니다.

---

## 실제 원인별 해결 방안

**가장 먼저 시도할 것 — Claude Code 버전 업데이트**

```bash
npm update -g @anthropic-ai/claude-code
```

오래된 버전의 Claude Code는 새 모델 식별자를 인식하지 못합니다. 대부분의 경우 이것 하나로 해결됩니다.

**버전 확인**

```bash
claude --version
```

**인증 재설정**

```bash
claude logout
claude login
```

토큰이 만료됐거나 인증 상태가 꼬인 경우입니다.

**캐시/설정 초기화**

```bash
rm -rf ~/.claude/
claude login
```

---

## 결론

문서가 제시하는 "3.x 모델로 다운그레이드" 방향은 올바른 해결책이 아닙니다. 오히려 **Claude Code 자체를 최신 버전으로 업데이트**하는 것이 정도(正道)입니다.

현재 어떤 환경(Mac/Windows/Linux)에서 사용 중이신가요? 그리고 `claude --version`으로 현재 버전을 확인해보실 수 있으신가요? 그에 맞게 더 구체적인 해결 경로를 잡아드릴 수 있습니다.

## Human (Message 3)

2.1.39 (Claude Code)

## Claude (Message 4)

좋습니다. 버전 정보 확인했습니다.

```
This block is not supported on your current device yet.
```

흥미로운 상황입니다. 검색 결과가 핵심적인 사실을 보여주고 있습니다.

---

## 진단 결과

현재 npm 최신 버전은 **2.1.39**로, 20시간 전에 배포됐습니다. 즉, CSP님은 이미 **최신 버전**을 사용 중입니다. 버전 문제가 아닙니다.

그리고 Anthropic이 공식적으로 **npm 설치 방식을 deprecated(지원 중단 예정)**로 선언했으며, Native Installer로 마이그레이션을 권장하고 있습니다.

이것이 모델 접근 오류의 실질적 배경입니다. npm 방식은 점점 뒷전으로 밀리고 있는 것이죠.

---

## 실제 해결 경로

### 지금 당장 — 인증 재확인

```bash
claude doctor
```

이 명령 하나로 현재 설치 상태, 인증, 모델 접근 권한을 한 번에 진단해줍니다.

### 근본 해결 — Native Installer로 마이그레이션

기존 npm 설치에서 네이티브 방식으로 마이그레이션하려면 다음 명령 하나로 됩니다.

```bash
claude install
```

네이티브 방식은 Node.js 의존성 없이 자동 업데이트도 지원합니다.

### 마이그레이션 전에 인증 문제가 의심되면

```bash
claude logout
claude login
```

---

## 한 줄 요약

버전은 문제없음. `claude doctor`로 진단 먼저, 그 다음 `claude install`로 네이티브 전환이 가장 깔끔한 해결책입니다.

`claude doctor` 결과를 붙여넣어 주시면 정확히 어디서 막혀있는지 바로 짚어드릴 수 있습니다.

## Human (Message 5)

C:\Users\Administrator>claude doctor
Diagnostics
└ Currently running: npm-global (2.1.39)
└ Path: C:\Program Files\nodejs\node.exe
└ Invoked: C:\Users\Administrator\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\cli.js
└ Config install method: unknown
└ Search: OK (vendor)
Updates
└ Auto-updates: enabled
└ Update permissions: Yes
└ Auto-update channel: latest
Version Locks
└ No active version locks

## Claude (Message 6)

Windows + npm-global 방식 확인됐습니다. 업데이트나 설치 자체는 정상이고, 문제는 **모델 접근 권한** 쪽입니다.

핵심 질문 하나 드립니다.

현재 Claude Code 인증 방식이 어떻게 되시나요?

```
This block is not supported on your current device yet.
```

인증 방식에 따라 해결 경로가 완전히 달라집니다.

- **OAuth (Pro/Max)** 방식이면 → 구독 플랜과 모델 접근 권한 연동 문제
- **API 키** 방식이면 → 키의 tier나 만료 여부 확인 필요
- **확인 필요**라면 → `claude` 실행 후 `/status` 명령으로 바로 확인 가능

## Human (Message 7)

질문: Claude Code 로그인 방식이 무엇인가요?
답변: Claude.ai Pro/Max 구독 (OAuth)

## Claude (Message 8)

Pro/Max OAuth 방식이군요. 그러면 원인이 거의 확정됩니다.

---

## 원인 진단

OAuth 방식은 Claude.ai 구독 플랜과 모델 접근이 **실시간으로 연동**됩니다. 모델 오류가 나는 패턴은 보통 세 가지입니다.

**1. OAuth 토큰 만료** — 가장 흔한 원인. 자동 갱신이 실패한 상태로 세션이 유지되는 경우입니다.

**2. 플랜 불일치** — Pro는 Sonnet/Haiku까지, Max는 Opus 포함. 설정에 Opus가 지정돼 있는데 Pro 플랜이면 오류납니다.

**3. `settings.json` 모델 고정** — 예전에 수동으로 모델을 설정해두고 잊어버린 경우입니다.

---

## 해결 순서

**Step 1 — 토큰 재발급 (80% 해결)**

```bash
claude logout
claude login
```

로그인 후 브라우저에서 claude.ai 인증 완료까지 기다리세요.

**Step 2 — 현재 상태 확인**

```bash
claude
/status
```

모델명과 플랜 정보가 표시됩니다.

**Step 3 — settings.json 확인 (모델 고정 여부)**

```bash
type C:\Users\Administrator\.claude\settings.json
```

여기서 `"model"` 키가 있으면 특정 모델로 고정된 겁니다. 해당 줄을 지우면 됩니다.

---

Step 1부터 시도해보시고 결과 알려주시면 바로 다음 단계 잡아드리겠습니다.

## Human (Message 9)

> Auto-compact true

    Show tips                                 true
    Reduce motion                             false
    Thinking mode                             true
    Prompt suggestions                        true
    Rewind code (checkpoints)                 true
    Verbose output                            false
    Terminal progress bar                     true
    Default permission mode                   Default
    Respect .gitignore in file picker         true
    Auto-update channel                       latest
    Theme                                     Dark mode
    Notifications                             Auto
    Output style                              default
    Language                                  Default (English)
    Editor mode                               normal
    Show code diff footer                     true
    Show PR status footer                     true
    Model                                     haiku
    Auto-connect to IDE (external terminal)   false
    Claude in Chrome enabled by default       true

Type to filter · Enter/↓ to select · Esc to clear

## Claude (Message 10)

발견했습니다. **바로 여기가 원인입니다.**

---

## 원인 확정

```
Model    haiku
```

`haiku`라는 **별칭(alias)** 으로 모델이 고정되어 있습니다. 이 별칭이 현재 버전에서 올바른 모델 식별자로 매핑되지 않아 오류가 발생하는 것입니다.

---

## 즉시 해결

이 `/config` 화면에서 바로 수정 가능합니다.

**Model 항목으로 커서를 이동 → Enter → 모델 선택 화면에서 변경**

추천 선택지는 플랜에 따라 다릅니다.

- **Pro 구독** → `claude-sonnet-4-5` 또는 `sonnet` 선택
- **Max 구독** → `claude-opus-4-5` 또는 `sonnet` 선택

또는 아예 **Model 항목을 비워두면(Default)** Claude Code가 자동으로 최적 모델을 선택합니다. Vibe coder 철학상 이게 가장 깔끔합니다. 설정해두고 잊어버리는 것보다 자동에 맡기는 편이 유지보수가 없습니다.

---

변경 후 `/status`로 모델 정상 인식되는지 확인해 주세요.

## Human (Message 11)

● There's an issue with the selected model (claude-sonnet-4-5-20250929). It may not exist or you may not have access to  
it.─Run─/model─to─pick─a─different─model.─────────────────────────────────────────────────────────────────────────────

## Claude (Message 12)

모델을 바꿨는데도 동일한 오류가 나오는군요. 이건 모델 설정 문제가 아니라 **인증/권한 레벨 문제**입니다.

---

## 원인 재진단

`claude-sonnet-4-5-20250929`는 정상적인 모델 식별자입니다. 그런데 "may not have access to it"이라는 메시지는 **계정이 이 모델에 접근할 권한이 없다**는 뜻입니다.

OAuth 방식에서 이런 오류가 나오는 경우는 거의 하나입니다.

**Claude.ai Pro/Max 구독이 현재 활성 상태가 아니거나, 결제 문제가 있는 경우.**

---

## 확인 순서

**Step 1 — claude.ai 웹에서 직접 확인**

브라우저에서 [claude.ai](https://claude.ai) 접속 → 로그인 → 실제로 대화가 되는지 확인하세요. 웹에서도 안 된다면 구독 문제입니다.

**Step 2 — 토큰 완전 재발급**

```bash
claude logout
claude login
```

로그인 완료 후 다시 시도.

**Step 3 — `/status`로 계정 정보 확인**

```bash
claude
/status
```

여기서 표시되는 계정명과 플랜 정보를 알려주시면 정확히 진단 가능합니다.

---

웹 claude.ai에서 정상적으로 대화가 되시나요?

## Human (Message 13)

잘되고 있자나

## Claude (Message 14)

그렇다면 구독은 정상입니다. 다른 원인이 있습니다.

```
This block is not supported on your current device yet.
```

```
This block is not supported on your current device yet.
```

원인을 찾았습니다. 생각보다 구조적인 문제입니다.

---

## 진짜 원인

OAuth 토큰은 약 **4일마다 만료**됩니다. 그런데 Claude Code가 만료된 토큰을 그냥 재사용하는 버그가 있어서, 토큰이 죽어있어도 만료 체크 없이 API 호출을 시도합니다.

결과적으로 모델 오류처럼 보이지만 실제로는 **인증 토큰이 조용히 죽어있는 상태**입니다.

---

## 해결 방법 (Windows 기준)

**Step 1 — 완전 초기화**

```bash
claude logout
```

**Step 2 — 캐시 파일 수동 삭제**

```bash
del C:\Users\Administrator\.claude.json
rmdir /s /q C:\Users\Administrator\.claude
```

**Step 3 — 재로그인**

```bash
claude login
```

브라우저가 열리면 claude.ai로 인증 완료까지 기다리세요. 브라우저가 자동으로 안 열리면 `c`를 눌러 URL을 복사한 뒤 브라우저에 직접 붙여넣으세요.

**Step 4 — 모델 설정을 Default로**

```
/config → Model → Default (비워두기)
```

---

이게 핵심입니다. 모델 문제가 아니라 토큰이 죽은 채로 모델 접근을 시도하니 "access" 오류로 표시된 것입니다. 한 번 해보시겠어요?

## Human (Message 15)

claude uninstall

## Claude (Message 16)

잠깐요! 혹시 완전히 제거하려는 건가요, 아니면 재설치 목적인가요?

`claude uninstall`은 Claude Code 자체를 삭제하는 명령입니다. 지금 상황에서는 **재설치 목적이라면 불필요한 과정**일 수 있습니다.

```
This block is not supported on your current device yet.
```

## Human (Message 17)

질문: claude uninstall 실행 의도가 무엇인가요?
답변: 완전 제거 후 재설치하려고

## Claude (Message 18)

좋습니다. 재설치 목적이면 올바른 방향입니다.

Windows npm 방식 기준으로 순서대로 진행하세요.

---

## 완전 제거 → 재설치 순서

**Step 1 — Claude Code 제거**

```bash
npm uninstall -g @anthropic-ai/claude-code
```

**Step 2 — 설정/캐시 파일 완전 삭제**

```bash
del C:\Users\Administrator\.claude.json
rmdir /s /q C:\Users\Administrator\.claude
npm cache clean --force
```

**Step 3 — 재설치**

```bash
npm install -g @anthropic-ai/claude-code
```

**Step 4 — 버전 확인**

```bash
claude --version
```

**Step 5 — 재로그인**

```bash
claude login
```

브라우저 인증창이 열리면 claude.ai 계정으로 완전히 인증 완료될 때까지 기다리세요.

**Step 6 — 모델 설정 확인**

```
/config → Model → Default (비워두기)
```

---

한 가지 팁을 드리자면, 이번 기회에 npm 방식 대신 **Native Installer**로 전환하는 것도 고려해볼 만합니다. npm은 현재 공식적으로 deprecated 방향이고, 네이티브 방식은 자동 업데이트도 더 안정적입니다. 재설치 후 안정화되면 `claude install` 명령으로 네이티브 전환이 가능합니다.

Step 2부터 진행하면서 막히는 부분 있으면 바로 알려주세요.

## Human (Message 19)

/model to try Opus 4.6

> login > login Generating… (20496d 2h 17m · ↓ 0 tokens)
> ● There's an issue with the selected model (claude-sonnet-4-5-20250929). It may not exist or you may not have access to it. Run /model to pick a ? for shortcuts
> different model.

## Claude (Message 20)

재설치 후에도 동일한 오류가 나오고 있군요. 이건 설치 문제가 아닙니다.

결정적인 단서가 두 가지 보입니다.

---

## 원인 확정

**단서 1 — `Generating… (20496d 2h 17m)`**

20496일 = 약 **56년**입니다. 타임스탬프가 완전히 깨져있습니다. 이건 OAuth 토큰 내부의 시간 정보가 손상된 신호입니다.

**단서 2 — `> login` 이 두 번 출력**

로그인 프로세스가 정상적으로 완료되지 않고 루프 상태입니다.

둘 다 같은 원인을 가리킵니다. **OAuth 인증이 실제로 완료되지 않은 상태**에서 Claude Code가 실행되고 있는 것입니다.

---

## 해결

**브라우저 인증이 핵심입니다.** `claude login` 실행 후 브라우저 창이 열렸을 때 claude.ai에서 인증을 완전히 마쳤나요?

다시 시도해보세요.

```bash
claude logout
claude login
```

터미널에서 로그인 URL이 나오면 `c`를 눌러 URL 복사 → 브라우저에 붙여넣기 → claude.ai 로그인 → 승인 버튼 클릭 → 터미널로 돌아와서 **"Logged in" 메시지 확인**까지 기다리세요.

브라우저 인증 완료 후 터미널에 어떤 메시지가 뜨는지 알려주시겠어요?

## Human (Message 21)

/model to try Opus 4.6 > /login ⎿ Login successful
✽ Stewing… > ok?
──────────────────────────────────────
● There's an issue with the selected model (claude-sonnet-4-5-20250929). It may not exist or you may not have access to it. Run /model to pick a  
different─model.────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

## Claude (Message 22)

"Login successful"이 확인됐는데도 동일한 오류가 나오고 있습니다. 이건 인증 문제가 아닙니다.

```
This block is not supported on your current device yet.
```

```
This block is not supported on your current device yet.
```

```
This block is not supported on your current device yet.
```

드디어 진짜 원인이 나왔습니다. 기술 문제가 아니라 **플랜 문제**였습니다.

---

## 원인 확정

**Pro 플랜($20/월)은 Claude Sonnet 4.5까지만 접근 가능하고, Opus는 Max 플랜($100~200/월) 전용입니다.**

그런데 `/config`에서 `haiku`로 모델이 고정되어 있다가 Sonnet으로 바꿔도 오류가 나는 건 이상합니다. 한 가지 더 확인이 필요합니다.

---

## 지금 당장 확인할 것

Claude Code 안에서 실행:

```
/status
```

여기서 **Plan** 항목에 `Pro`인지 `Max`인지 표시됩니다. 그리고 `/model` 실행하면 현재 선택 가능한 모델 목록이 나옵니다. 그 목록에 `sonnet`이 있는지 확인해주세요.

---

## 만약 Pro 플랜이라면

Sonnet은 접근 가능해야 정상입니다. 그런데 Sonnet도 안 된다면 **사용량 한도 초과** 가능성이 있습니다.

Pro 플랜은 5시간마다 약 45메시지 한도가 리셋됩니다.

`/usage` 명령으로 현재 사용량 확인해보세요.

```
/usage
```

`/status`와 `/usage` 결과를 알려주시면 정확히 진단할 수 있습니다.

## Human (Message 23)

Version: 2.1.39
Session name: /rename to add a name
Session ID: 7ec65d84-a780-4288-af41-3714c9d462c5
cwd: C:\Users\Administrator
Auth token: ANTHROPIC_AUTH_TOKEN
Anthropic base URL: http://localhost:11434
Model: Default (claude-sonnet-4-5-20250929)
Memory:
Setting sources:
Esc to cancel

## Claude (Message 24)

찾았습니다. **완전히 다른 원인이었습니다.**

---

## 진짜 원인

```
Anthropic base URL: http://localhost:11434
```

**localhost:11434** — 이건 Anthropic 서버가 아니라 **Ollama** 로컬 서버 주소입니다.

Claude Code가 Anthropic API 대신 로컬 Ollama 서버로 요청을 보내고 있었던 겁니다. Ollama는 `claude-sonnet-4-5-20250929` 같은 Claude 모델을 모르니 "access" 오류가 발생한 것이고, 56년짜리 타임스탬프도 여기서 나온 것입니다.

---

## 해결

환경변수에서 `ANTHROPIC_BASE_URL`이 설정되어 있습니다. 이걸 제거하면 됩니다.

**Windows 환경변수 확인 및 제거:**

```bash
# 현재 설정 확인
echo %ANTHROPIC_BASE_URL%

# 현재 세션에서 제거
set ANTHROPIC_BASE_URL=

# 영구 제거 (시스템 환경변수)
setx ANTHROPIC_BASE_URL ""
```

또는 **Windows 설정** → 시스템 → 고급 시스템 설정 → 환경변수 → `ANTHROPIC_BASE_URL` 찾아서 삭제.

제거 후 터미널을 완전히 닫고 다시 열어서 `claude` 실행해보세요. `/status`에서 `Anthropic base URL`이 사라지면 정상입니다.

## Human (Message 25)

/model to try Opus 4.6

> ok?
> ⎿  API Error: 401 {"type":"error","error":{"type":"authentication_error","message":"Invalid bearer

     token"},"request_id":"req_011CY3NsrKp2SwkA19ayhAy6"} · Please run /login

> /login
> ⎿ Login successful
> /model
> ⎿ Set model to Default (claude-sonnet-4-5-20250929)
> ✻ Pontificating… > ok?
> ⎿  API Error: 401 {"type":"error","error":{"type":"authentication_error","message":"Invalid bearer ───────────────────

     token"},"request_id":"req_011CY3NwawTirHt4t2Vt6yH7"} · Please run /login

## Claude (Message 26)

거의 다 왔습니다. `ANTHROPIC_BASE_URL`은 제거됐는데 이번엔 `ANTHROPIC_AUTH_TOKEN` 문제입니다.

---

## 원인

`/status`에서 봤던 이 줄이 핵심입니다:

```
Auth token: ANTHROPIC_AUTH_TOKEN
```

환경변수로 `ANTHROPIC_AUTH_TOKEN`이 설정되어 있고, 그 값이 **잘못된 토큰**입니다. OAuth 로그인을 해도 이 환경변수가 우선 적용되어 계속 401이 나오는 것입니다.

---

## 해결

```bash
# 현재 값 확인
echo %ANTHROPIC_AUTH_TOKEN%

# 현재 세션에서 제거
set ANTHROPIC_AUTH_TOKEN=

# 영구 제거
setx ANTHROPIC_AUTH_TOKEN ""
```

또는 **Windows 환경변수 설정** → `ANTHROPIC_AUTH_TOKEN` 찾아서 완전히 삭제.

삭제 후 터미널 완전히 닫고 새로 열어서:

```bash
claude login
claude
/status
```

`/status`에서 `Auth token: oauth` 또는 `Auth token: claude.ai`로 표시되면 정상입니다.

## Human (Message 27)

PS C:\Users\Administrator> setx ANTHROPIC_AUTH_TOKEN ""
오류: 구문이 잘못되었습니다.
사용법을 보려면 "SETX /?"를 입력하십시오.

## Claude (Message 28)

`setx`는 빈 값을 못 받습니다. 다른 방법으로 제거하세요.

---

## 방법 1 — PowerShell로 제거 (권장)

```powershell
[Environment]::SetEnvironmentVariable("ANTHROPIC_AUTH_TOKEN", $null, "User")
[Environment]::SetEnvironmentVariable("ANTHROPIC_AUTH_TOKEN", $null, "Machine")
[Environment]::SetEnvironmentVariable("ANTHROPIC_BASE_URL", $null, "User")
[Environment]::SetEnvironmentVariable("ANTHROPIC_BASE_URL", $null, "Machine")
```

`ANTHROPIC_BASE_URL`도 같이 완전히 제거하는 겁니다.

---

## 방법 2 — GUI로 제거

1. `Win + R` → `sysdm.cpl` 입력 → Enter
2. **고급** 탭 → **환경 변수** 클릭
3. 위쪽(사용자 변수)과 아래쪽(시스템 변수) 양쪽에서 `ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_BASE_URL` 찾아서 **삭제**

---

PowerShell 방법 실행 후 터미널 완전히 닫고 새로 여세요. 그 다음 결과 알려주시면 됩니다.

## Human (Message 29)

ok?

## Claude (Message 30)

터미널 새로 열고 아래 순서로 확인해주세요.

```powershell
# 1. 환경변수 제거됐는지 확인
echo $env:ANTHROPIC_AUTH_TOKEN
echo $env:ANTHROPIC_BASE_URL

# 2. 둘 다 아무것도 안 나오면 로그인
claude login

# 3. 상태 확인
claude
/status
```

`/status`에서 어떻게 나오는지 붙여넣어 주시면 최종 확인하겠습니다.

---

_Archived from: `542c1e6f-745c-47b4-ac42-2638df92522c.json`_
