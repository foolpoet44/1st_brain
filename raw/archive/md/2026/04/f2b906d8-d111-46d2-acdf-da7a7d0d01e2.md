---
title: 무료 모델 자동전환 시스템 구성
date: 2026-04-05
time: 07:39
uuid: f2b906d8-d111-46d2-acdf-da7a7d0d01e2
type: conversation
tags: [archive, conversation]
---

# 무료 모델 자동전환 시스템 구성

**날짜**: 2026-04-05 07:39

---

## Human (Message 1)

클로드 코드의 사용량 세션 도달시 무료 모델로 자동전환하는 체계를 통해 24시간 구동되는 환경 구성

## Claude (Message 2)

# Claude Code 24시간 무중단 구동 — 사용량 제한 자동 우회 체계

CSP가 구상하는 것의 본질을 먼저 짚고 가겠습니다. Claude Code는 Anthropic의 유료 API를 소비하는 구조라, 세션 한도나 크레딧 소진 시 멈추게 됩니다. 이를 "무료 모델로 자동 전환"하여 24시간 연속 구동하려는 발상은 엔지니어링적으로 흥미롭지만, 몇 가지 현실적 레이어를 함께 짚어야 합니다.

---

## 1. 아키텍처 개념 설계

```
┌─────────────────────────────────────────────────────┐
│              Orchestrator (Node.js / Python)         │
│                                                      │
│  ┌──────────┐    limit    ┌───────────────────────┐  │
│  │ Claude   │ ──detected──▶  Model Router          │  │
│  │  Code    │             │  (Fallback Manager)   │  │
│  │ (Primary)│             └───────────┬───────────┘  │
│  └──────────┘                         │              │
│                          ┌────────────▼────────────┐ │
│                          │  Free / Alt Models      │ │
│                          │  - Gemini 2.0 Flash     │ │
│                          │  - Ollama (local)       │ │
│                          │  - Groq (free tier)     │ │
│                          └─────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

핵심 패턴은 **Circuit Breaker + Strategy Pattern**입니다. Claude Code가 rate limit 또는 usage limit 에러를 던지는 순간, Orchestrator가 이를 감지하고 대체 모델 전략으로 자동 스위치합니다.

---

## 2. 실제 구현 가능한 접근법

### Option A — Claude Code + Gemini Flash 하이브리드 (추천)

Claude Code의 `--model` 플래그와 환경변수를 활용하는 방식입니다.

```bash
# ~/.config/claude/model-fallback.sh

#!/bin/bash

PRIMARY_MODEL="claude-opus-4-5"
FALLBACK_MODEL="claude-haiku-4-5-20251001"  # 저렴한 tier
FREE_FALLBACK="gemini-2.0-flash"            # Google AI Studio 무료

run_with_fallback() {
  local prompt="$1"
  
  # Primary 시도
  result=$(claude --model $PRIMARY_MODEL -p "$prompt" 2>&1)
  exit_code=$?
  
  # Rate limit 감지 (exit code 또는 stderr 패턴)
  if echo "$result" | grep -q "rate_limit\|usage_limit\|529\|overloaded"; then
    echo "[FALLBACK] Switching to $FALLBACK_MODEL..."
    result=$(claude --model $FALLBACK_MODEL -p "$prompt" 2>&1)
    exit_code=$?
  fi
  
  # 완전 소진 시 외부 무료 모델
  if [ $exit_code -ne 0 ]; then
    echo "[FREE_FALLBACK] Switching to Gemini Flash..."
    result=$(call_gemini_flash "$prompt")
  fi
  
  echo "$result"
}
```

### Option B — Node.js Orchestrator (더 정교한 제어)

```javascript
// model-router.js

import Anthropic from '@anthropic-ai/sdk';
import { GoogleGenerativeAI } from '@google/generative-ai';

const MODEL_PRIORITY = [
  { provider: 'anthropic', model: 'claude-opus-4-5', tier: 'primary' },
  { provider: 'anthropic', model: 'claude-haiku-4-5-20251001', tier: 'budget' },
  { provider: 'google', model: 'gemini-2.0-flash', tier: 'free' },
  { provider: 'groq', model: 'llama-3.3-70b', tier: 'free' },
  { provider: 'ollama', model: 'qwen2.5-coder', tier: 'local' },
];

class ModelRouter {
  constructor() {
    this.currentIndex = 0;
    this.cooldowns = new Map(); // 모델별 쿨다운 추적
  }

  async complete(prompt, systemPrompt = '') {
    for (let i = this.currentIndex; i < MODEL_PRIORITY.length; i++) {
      const modelConfig = MODEL_PRIORITY[i];
      
      if (this.isInCooldown(modelConfig.model)) continue;
      
      try {
        const result = await this.callModel(modelConfig, prompt, systemPrompt);
        this.currentIndex = i; // 성공한 모델 기억
        return result;
      } catch (err) {
        if (this.isRateLimitError(err)) {
          // 쿨다운 설정 (Anthropic: 60초, 등)
          this.setCooldown(modelConfig.model, this.getCooldownDuration(err));
          console.log(`[Router] ${modelConfig.model} rate limited, trying next...`);
          continue;
        }
        throw err; // 다른 에러는 상위로
      }
    }
    throw new Error('All models exhausted');
  }

  isRateLimitError(err) {
    return err.status === 429 || 
           err.status === 529 ||
           err.message?.includes('rate_limit') ||
           err.message?.includes('overloaded');
  }

  getCooldownDuration(err) {
    // retry-after 헤더 파싱
    const retryAfter = err.headers?.['retry-after'];
    return retryAfter ? parseInt(retryAfter) * 1000 : 60000;
  }

  async callModel(config, prompt, system) {
    switch(config.provider) {
      case 'anthropic':
        return this.callAnthropic(config.model, prompt, system);
      case 'google':
        return this.callGemini(config.model, prompt, system);
      case 'groq':
        return this.callGroq(config.model, prompt, system);
      case 'ollama':
        return this.callOllama(config.model, prompt, system);
    }
  }
}
```

### Option C — Claude Code 자체의 `/model` 전환 자동화

Claude Code 세션 내에서 직접 모델을 전환하는 방식:

```bash
# tmux 기반 세션 관리 + 자동 재시작

#!/bin/bash
# claude-watchdog.sh

SESSION="csp-agent"
LOG="/tmp/claude-usage.log"

start_claude() {
  local model="${1:-claude-opus-4-5}"
  tmux send-keys -t $SESSION \
    "claude --model $model" Enter
}

monitor_usage() {
  while true; do
    # Claude Code 로그에서 limit 신호 감지
    if tail -n 5 $LOG | grep -q "Claude AI usage limit reached"; then
      echo "[$(date)] Usage limit detected, switching model..."
      
      # 현재 세션 정리
      tmux send-keys -t $SESSION "/model claude-haiku-4-5-20251001" Enter
      sleep 2
      
      # 그래도 안되면 세션 재시작 with 대체 모델
      if tail -n 2 $LOG | grep -q "limit"; then
        tmux send-keys -t $SESSION "exit" Enter
        sleep 1
        start_claude "claude-haiku-4-5-20251001"
      fi
    fi
    sleep 30
  done
}
```

---

## 3. 무료 대체 모델 선택 기준

| 모델 | 특성 | CSP 사용 케이스 |
|------|------|----------------|
| **Gemini 2.0 Flash** | Google AI Studio 무료, 빠름 | 단순 코드 생성, 요약 |
| **Groq + Llama 3.3** | 초고속, 무료 티어 | 반복 작업, 분류 |
| **Ollama (로컬)** | 완전 무료, 오프라인 | 민감 데이터 처리 |
| **Claude Haiku** | 저렴 ($0.25/MTok), 빠름 | Budget fallback |

---

## 4. CSP 환경 권장 스택

CSP의 현재 스택(Node.js + Claude Code + tmux)을 고려하면:

```
Primary:   Claude Sonnet/Opus  (Claude Code MAX 세션)
Budget:    Claude Haiku         (API 직접 호출, 저렴)  
Free Alt:  Gemini 2.0 Flash    (Google AI Studio 무료 키)
Local:     Ollama + Qwen2.5    (완전 오프라인 fallback)
```

이 4계층 구조가 실질적인 24시간 무중단을 가능하게 합니다. Claude Code MAX 플랜 기준으로도 heavy usage 시 rate limit이 걸리므로, **Haiku를 budget fallback으로 두는 것이 가장 비용 효율적**입니다.

---

구체적으로 어떤 워크로드를 24시간 돌리려는 건지 알면 (CSP Daily Radar 자동화인지, ESCON 에이전트인지, Pulse Check 자동화인지) 그에 맞는 더 구체적인 구현 설계를 잡아드릴 수 있습니다.

## Human (Message 3)

클로드 코드에서 gemini 2.0 사용하는 방법

## Claude (Message 4)

# Claude Code에서 Gemini 2.0 사용하기
```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```


```
This block is not supported on your current device yet.
```

현재 상황을 먼저 명확히 짚는 게 중요합니다.

---

## 핵심 전제: Claude Code는 공식적으로 Gemini를 지원하지 않는다

Claude Code는 Anthropic 모델 전용으로 설계되었고, `ANTHROPIC_BASE_URL` 환경변수를 통해 프록시 서버를 거치는 우회 방법만 존재합니다. 방법은 크게 세 가지입니다.

---

## 방법 1 — Bifrost (가장 빠른 세팅, 추천)

Bifrost는 Claude Code의 Anthropic API 요청을 가로채서 Gemini API 형식으로 변환한 뒤 Google 엔드포인트로 라우팅하고, 응답을 다시 Anthropic 형식으로 돌려주는 프로토콜 변환 레이어입니다. Claude Code 입장에서는 Anthropic과 통신하는 것처럼 동작합니다.

```bash
# Step 1: Bifrost 실행 (npx 한 줄)
GEMINI_API_KEY=your_key npx -y @maximhq/bifrost

# Step 2: Claude Code를 Bifrost로 연결
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude

# Step 3: 세션 내에서 실시간 모델 전환
/model gemini/gemini-2.0-flash
/model gemini/gemini-2.5-pro
/model anthropic/claude-sonnet-4-5-20250929  # Claude로 복귀
```

세션 내에서 `/model` 커맨드로 Claude와 Gemini를 실시간으로 A/B 비교할 수 있습니다. 단, Claude Code의 Extended Thinking, Web Search, Computer Use 같은 Anthropic 전용 기능은 Gemini 라우팅 시 작동하지 않습니다.

---

## 방법 2 — LiteLLM Proxy (가장 안정적, 세밀한 제어)

LiteLLM은 Anthropic Messages API 형식을 유지하면서 자동으로 다양한 프로바이더 형식으로 변환합니다.

```bash
# 설치
pip install litellm

# config.yaml 작성
cat > litellm_config.yaml << EOF
model_list:
  - model_name: gemini-2.0-flash
    litellm_params:
      model: gemini/gemini-2.0-flash
      api_key: os.environ/GEMINI_API_KEY
  - model_name: gemini-2.5-pro
    litellm_params:
      model: gemini/gemini-2.5-pro
      api_key: os.environ/GEMINI_API_KEY
EOF

# 프록시 서버 시작
export GEMINI_API_KEY=your_key
export LITELLM_MASTER_KEY=sk-1234   # 임의 설정
litellm --config litellm_config.yaml

# Claude Code 연결
ANTHROPIC_BASE_URL=http://localhost:4000 \
ANTHROPIC_API_KEY=sk-1234 \
claude --model gemini-2.0-flash
```

---

## 방법 3 — claude-code-proxy (가장 단순, GitHub 오픈소스)

GEMINI_API_KEY와 PREFERRED_PROVIDER를 설정하면 Haiku → Small Model, Sonnet/Opus → Big Model로 자동 매핑됩니다.

```bash
# Docker 방식
cat > .env << EOF
GEMINI_API_KEY=your_gemini_key
OPENAI_API_KEY=fallback_key   # fallback용
PREFERRED_PROVIDER=google
BIG_MODEL=gemini-2.5-pro
SMALL_MODEL=gemini-2.0-flash
EOF

docker run -p 8082:8082 --env-file .env \
  ghcr.io/1rgs/claude-code-proxy:latest

# Claude Code 연결
ANTHROPIC_BASE_URL=http://localhost:8082 claude
```

---

## CSP 24시간 구동용 권장 세팅

CSP가 원하는 "usage limit 시 자동 전환" 시나리오라면 Bifrost가 가장 적합합니다. Bifrost는 자동 failover 기능을 내장하고 있어서 Gemini를 primary로, Claude를 fallback으로 설정하거나 그 반대 구성도 가능합니다.

```bash
# ~/.zshrc에 추가해두면 항상 적용
export GEMINI_API_KEY="your_google_ai_studio_key"

# alias 등록
alias claude-gemini='ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude'
alias bifrost='GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost &'
```

Google AI Studio에서 Gemini API 키는 무료로 발급됩니다 (aistudio.google.com). Gemini 2.0 Flash 기준으로 분당 15회, 일 1,500회까지 무료 tier가 적용되므로, Claude Code limit 소진 시 실질적인 무료 fallback으로 활용 가능합니다.

## Human (Message 5)

# ~/.zshrc에 설정하는 방법

## Claude (Message 6)

가장 실용적인 방법으로 정리해드립니다.

---

## ~/.zshrc 설정 방법

### Step 1 — zshrc 열기

```bash
nano ~/.zshrc
# 또는
code ~/.zshrc
# 또는
vi ~/.zshrc
```

### Step 2 — 아래 내용 추가

```bash
# ─── Gemini / Claude Code Proxy ───────────────────────────
export GEMINI_API_KEY="your_google_ai_studio_key_here"
export ANTHROPIC_API_KEY="your_anthropic_key_here"

# Bifrost 백그라운드 실행 함수
bifrost-start() {
  if pgrep -f "maximhq/bifrost" > /dev/null; then
    echo "✅ Bifrost already running"
  else
    GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost &
    echo "🚀 Bifrost started (port 8080)"
    sleep 2
  fi
}

# Claude → Gemini 모드
alias claude-gemini='bifrost-start && ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude'

# 일반 Claude (Anthropic 직접)
alias claude-direct='ANTHROPIC_BASE_URL="" claude'

# Bifrost 종료
alias bifrost-stop='pkill -f "maximhq/bifrost" && echo "🛑 Bifrost stopped"'
# ───────────────────────────────────────────────────────────
```

### Step 3 — 적용

```bash
source ~/.zshrc
```

---

## 사용법

```bash
# Gemini로 Claude Code 실행
claude-gemini

# 세션 안에서 모델 전환
/model gemini/gemini-2.0-flash
/model gemini/gemini-2.5-pro
/model anthropic/claude-sonnet-4-5-20250929

# 원래 Claude로
claude-direct
```

---

## Google AI Studio 키 발급

1. [aistudio.google.com](https://aistudio.google.com) 접속
2. **Get API Key** → **Create API Key**
3. 복사 후 `your_google_ai_studio_key_here` 자리에 붙여넣기

무료 tier 한도는 Gemini 2.0 Flash 기준 **분당 15회 / 일 1,500회**입니다.

## Human (Message 7)

anthrophic api key 없이 계정의 할당토큰 사용하기

## Claude (Message 8)

좋은 질문입니다. 이건 Claude Code의 인증 구조를 이해하면 바로 답이 나옵니다.

---

## Claude Code는 원래 API Key 없이 동작한다

Claude Code는 `claude login` 으로 claude.ai 계정에 로그인하면, **API Key 없이 계정의 구독 토큰(Max 플랜 등)을 그대로 사용**합니다.

```bash
# 최초 1회만 실행
claude login
```

브라우저가 열리면서 claude.ai 계정으로 OAuth 인증 → 완료.

이후 `ANTHROPIC_API_KEY` 환경변수가 없어도 Claude Code가 정상 동작합니다.

---

## 확인 방법

```bash
# 현재 인증 상태 확인
claude doctor

# 어떤 계정으로 로그인되어 있는지
claude auth status
```

---

## Bifrost + 계정 토큰 조합

문제는 **Bifrost를 쓸 때** 입니다. Bifrost는 `ANTHROPIC_API_KEY`를 요구하는 구조라서 계정 토큰과 충돌할 수 있습니다.

이 경우 해결책은 두 가지입니다.

**방법 A — ANTHROPIC_API_KEY를 더미값으로 설정**

```bash
# ~/.zshrc
# 계정 로그인 상태면 실제 API Key 불필요
# Bifrost 연결 시 더미값으로 에러 우회
alias claude-gemini='bifrost-start && \
  ANTHROPIC_BASE_URL=http://localhost:8080/anthropic \
  ANTHROPIC_API_KEY=dummy \
  claude'
```

Bifrost가 실제 Anthropic에 요청을 보내지 않고 Gemini로 라우팅하므로, API Key 검증이 사실상 의미 없어집니다.

**방법 B — claude login 상태 그대로 Bifrost 없이 사용 (가장 단순)**

```bash
# 그냥 이걸로 충분
claude
```

Claude Max 플랜이면 별도 API Key 없이 계정 할당량 내에서 무제한에 가깝게 사용 가능합니다. Gemini fallback이 필요한 순간에만 `claude-gemini`로 전환하는 방식이 CSP 환경에서는 가장 현실적입니다.

---

## 요약

| 상황 | 방법 |
|------|------|
| 일반 사용 | `claude login` 후 그냥 `claude` |
| API Key 필요 여부 | ❌ 불필요 (계정 토큰 자동 사용) |
| Gemini fallback 시 | `ANTHROPIC_API_KEY=dummy` 더미값으로 우회 |
| 인증 확인 | `claude doctor` |

## Human (Message 9)

적용하는 상세 방법

## Claude (Message 10)

단계별로 완전히 처음부터 진행합니다.

---

## Step 1 — Claude Code 로그인 확인

```bash
# 현재 상태 확인
claude doctor
```

`Logged in: Yes` 가 보이면 이미 완료. 아니라면:

```bash
claude login
# 브라우저 열림 → claude.ai 계정으로 로그인
```

---

## Step 2 — Google AI Studio API Key 발급

1. [aistudio.google.com](https://aistudio.google.com) 접속
2. 좌측 메뉴 **Get API Key** 클릭
3. **Create API key** → 키 복사해서 메모장에 임시 보관

---

## Step 3 — ~/.zshrc 열기

```bash
nano ~/.zshrc
```

터미널에 편집 화면이 열립니다.

---

## Step 4 — 파일 맨 아래에 붙여넣기

키보드 `Ctrl + End` 또는 방향키로 맨 아래로 이동 후 아래 내용 추가:

```bash
# ─── Claude Code + Gemini Fallback ────────────────────────

export GEMINI_API_KEY="여기에_구글_AI_스튜디오_키_붙여넣기"

# Bifrost 시작 함수
bifrost-start() {
  if pgrep -f "maximhq/bifrost" > /dev/null; then
    echo "✅ Bifrost already running"
  else
    GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost > /tmp/bifrost.log 2>&1 &
    echo "🚀 Bifrost starting..."
    sleep 3
  fi
}

# Gemini 모드 (계정 토큰 사용 + Gemini 라우팅)
alias claude-gemini='bifrost-start && ANTHROPIC_BASE_URL=http://localhost:8080/anthropic ANTHROPIC_API_KEY=dummy claude'

# 일반 모드 (계정 토큰 직접 사용)
alias claude-normal='ANTHROPIC_BASE_URL="" claude'

# Bifrost 종료
alias bifrost-stop='pkill -f "maximhq/bifrost" && echo "🛑 Bifrost stopped"'

# 상태 확인
alias bifrost-status='pgrep -f "maximhq/bifrost" > /dev/null && echo "✅ Running" || echo "❌ Stopped"'

# ──────────────────────────────────────────────────────────
```

---

## Step 5 — 저장 및 나가기

nano 기준:
- `Ctrl + O` → 저장
- `Enter` → 확인
- `Ctrl + X` → 종료

---

## Step 6 — 적용

```bash
source ~/.zshrc
```

---

## Step 7 — 동작 확인

```bash
# 일반 Claude (계정 토큰)
claude-normal

# Gemini 모드
claude-gemini
# → 세션 내에서 /model gemini/gemini-2.0-flash 입력

# Bifrost 상태 확인
bifrost-status
```

---

## 문제 발생 시

```bash
# Bifrost 로그 확인
tail -f /tmp/bifrost.log

# Bifrost 재시작
bifrost-stop
claude-gemini
```

---

이게 전부입니다. 이후로는 Claude 사용량 한도 근접 시 `claude-gemini` 로 전환하면 Gemini 2.0 Flash 무료 티어로 자동 라우팅됩니다.

## Human (Message 11)

(base) dkmac@MacBook-Air-3 ~ % claude login
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                    
❯ login                                                                                                             
  ⎿  Please run /login · API Error: 401 {"type":"error","error":{"type":"authentication_error","message":"Invalid   
     authentication credentials"},"request_id":"req_011CZkFvLFTAhkTXwoWK6MkC"}

## Claude (Message 12)

이미 로그인된 상태입니다. 에러 메시지를 자세히 보면:

```
Opus 4.6 with medium effort · Claude Pro
```

**Claude Pro 계정으로 이미 정상 로그인**되어 있습니다. `claude login`을 또 실행하려다가 충돌이 난 것입니다.

---

## 현재 상태 확인

```bash
# 로그인 상태 정상 확인
claude doctor
```

정상이면 아래처럼 보입니다:

```
✅ Claude Code is properly configured
✅ Logged in as: your@email.com
```

---

## 바로 사용 가능

```bash
# 그냥 실행하면 됩니다
claude
```

API Key 없이 Claude Pro 구독 토큰으로 동작 중입니다.

---

## Gemini Fallback 세팅도 그대로 진행

Step 3~7 그대로 진행하시면 됩니다. `claude login`은 이미 완료된 상태이므로 건너뛰고 바로 `~/.zshrc` 편집부터 시작하면 됩니다.

```bash
nano ~/.zshrc
```

## Human (Message 13)

claude-gemini 설정 방법

## Claude (Message 14)

현재 상태에서 바로 진행합니다.

---

## Step 1 — Google AI Studio API Key 발급

[aistudio.google.com](https://aistudio.google.com) 접속 → **Get API Key** → **Create API key** → 복사

---

## Step 2 — zshrc 편집

```bash
nano ~/.zshrc
```

---

## Step 3 — 맨 아래에 추가

```bash
# ─── Claude Code + Gemini Fallback ────────────────────────

export GEMINI_API_KEY="AIza로_시작하는_키_여기에_붙여넣기"

# Bifrost 시작 함수
bifrost-start() {
  if pgrep -f "maximhq/bifrost" > /dev/null; then
    echo "✅ Bifrost already running"
  else
    GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost > /tmp/bifrost.log 2>&1 &
    echo "🚀 Bifrost starting..."
    sleep 3
  fi
}

# Gemini 모드
alias claude-gemini='bifrost-start && ANTHROPIC_BASE_URL=http://localhost:8080/anthropic ANTHROPIC_API_KEY=dummy claude'

# 일반 모드 (Claude Pro 계정 토큰)
alias claude-normal='ANTHROPIC_BASE_URL="" claude'

# 유틸
alias bifrost-stop='pkill -f "maximhq/bifrost" && echo "🛑 Bifrost stopped"'
alias bifrost-status='pgrep -f "maximhq/bifrost" > /dev/null && echo "✅ Running" || echo "❌ Stopped"'
alias bifrost-log='tail -f /tmp/bifrost.log'

# ──────────────────────────────────────────────────────────
```

---

## Step 4 — 저장

```
Ctrl + O  →  Enter  →  Ctrl + X
```

---

## Step 5 — 적용 및 테스트

```bash
# 적용
source ~/.zshrc

# Gemini 모드 실행
claude-gemini

# 세션 안에서 모델 확인/전환
/model gemini/gemini-2.0-flash
```

---

## 안 되면 로그 확인

```bash
bifrost-log
```

여기서 에러 메시지 보내주시면 바로 해결해드립니다.

## Human (Message 15)

(base) dkmac@MacBook-Air-3 ~ % bifrost-log
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"governance store initialized successfully"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"async job executor initialized"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"bifrost client initialized"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"listing all models and adding to model catalog"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"models added to catalog"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"plugin status: logging - active"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"plugin status: governance - active"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"plugin status: telemetry - active"}
{"level":"info","time":"2026-04-05T16:59:01+09:00","message":"successfully started bifrost, serving UI on http://localhost:8080"}
{"level":"info","time":"2026-04-05T16:59:02+09:00","message":"model-parameters-sync: successfully synced 9840 model parameters records"}

## Claude (Message 16)

Bifrost 완벽하게 실행 중입니다. 이제 Claude Code 연결만 하면 됩니다.

---

## 지금 바로 실행

새 터미널 탭 열고:

```bash
claude-gemini
```

---

## 세션 안에서 모델 전환

```bash
/model gemini/gemini-2.0-flash
```

또는

```bash
/model gemini/gemini-2.5-pro
```

---

## Bifrost UI도 확인 가능

브라우저에서 [http://localhost:8080](http://localhost:8080) 접속하면 요청 로그, 모델 상태 등 대시보드가 보입니다.

---

정상적으로 `claude-gemini` 세션이 열리면 성공입니다. 혹시 연결 후 에러가 나면 메시지 보내주세요.

## Human (Message 17)

❯ /model gemini/gemini-2.0-flash                                                                                    
  ⎿  API error: 500 {"type":"error","error":{"type":"api_error","message":"failed to get config for provider: not   
     found"}}

## Claude (Message 18)

Bifrost에서 Gemini 프로바이더 설정이 누락된 상태입니다. Bifrost는 설정 파일이 필요합니다.

---

## 해결 방법 — Bifrost config 파일 생성

```bash
# config 디렉토리 생성
mkdir -p ~/.bifrost

# config 파일 작성
cat > ~/.bifrost/config.toml << EOF
[providers]
  [providers.gemini]
    api_key = "$GEMINI_API_KEY"
EOF
```

---

## Bifrost 재시작

```bash
# 기존 종료
bifrost-stop

# 새 터미널에서 config 지정하여 재시작
GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost --config ~/.bifrost/config.toml > /tmp/bifrost.log 2>&1 &
sleep 3

# Claude Gemini 모드 실행
claude-gemini
```

---

## ~/.zshrc의 bifrost-start 함수도 업데이트

```bash
nano ~/.zshrc
```

기존 `bifrost-start()` 부분을 아래로 교체:

```bash
bifrost-start() {
  if pgrep -f "maximhq/bifrost" > /dev/null; then
    echo "✅ Bifrost already running"
  else
    GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost \
      --config ~/.bifrost/config.toml \
      > /tmp/bifrost.log 2>&1 &
    echo "🚀 Bifrost starting..."
    sleep 3
  fi
}
```

저장 후:

```bash
source ~/.zshrc
```

---

다시 `/model gemini/gemini-2.0-flash` 입력 후 결과 알려주세요.

## Human (Message 19)

(base) dkmac@MacBook-Air-3 ~ % claude-gemini                      
[3] 64774
🚀 Bifrost starting...
[3]  + exit 1     GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost > /tmp/bifrost.log 2>&
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · API Usage Billing
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                    
                                                                                                                    
 ⚠Auth conflict: Both a token (claude.ai) and an API key (ANTHROPIC_API_KEY) are set. This may lead to unexpected   
  behavior.                                                                                                         
    · Trying to use claude.ai? Unset the ANTHROPIC_API_KEY environment variable, or claude /logout then say "No" to 
    the API key approval before login.
    · Trying to use ANTHROPIC_API_KEY? claude /logout to sign out of claude.ai.
                                                                                                                    
❯ /model                                                                                                            
  ⎿  Kept model as Opus 4.6                                                                                         
                                                                                                                    
❯  /model gemini/gemini-2.0-flash                                                                                   
  ⎿  API Error: 400 {"type":"error","error":{"type":"api_error","message":"provider is required"}}

## Claude (Message 20)

두 가지 문제가 동시에 발생했습니다. 순서대로 해결합니다.

---

## 문제 1 — Auth 충돌

`ANTHROPIC_API_KEY=dummy` 가 Claude Pro 계정 토큰과 충돌 중입니다.

```bash
# zshrc에서 alias 수정
nano ~/.zshrc
```

기존:
```bash
alias claude-gemini='bifrost-start && ANTHROPIC_BASE_URL=http://localhost:8080/anthropic ANTHROPIC_API_KEY=dummy claude'
```

변경:
```bash
alias claude-gemini='bifrost-start && ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude'
```

저장 후 적용:
```bash
source ~/.zshrc
```

---

## 문제 2 — Bifrost exit 1 (설정 파일 문제)

Bifrost가 `--config` 옵션을 지원하지 않는 버전일 수 있습니다. 환경변수 방식으로 변경합니다.

```bash
# 기존 Bifrost 전부 종료
pkill -f bifrost
pkill -f maximhq

# 로그 확인
cat /tmp/bifrost.log
```

로그 내용 보내주시면 정확한 원인 파악 가능합니다.

---

## 임시 직접 실행으로 테스트

```bash
# 터미널 1: Bifrost 직접 실행
GEMINI_API_KEY="AIza로시작하는실제키" npx -y @maximhq/bifrost

# 터미널 2: Claude 연결
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
```

Bifrost 실행 터미널에서 에러 메시지 그대로 보내주세요.

## Human (Message 21)

(base) dkmac@MacBook-Air-3 ~ % claude-gemini
[3] 66047
🚀 Bifrost starting...
[3]  + exit 1     GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost > /tmp/bifrost.log 2>&
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · API Usage Billing
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                    
                                                                                                                    
 ⚠Auth conflict: Both a token (claude.ai) and an API key (ANTHROPIC_API_KEY) are set. This may lead to unexpected   
  behavior.                                                                                                         
    · Trying to use claude.ai? Unset the ANTHROPIC_API_KEY environment variable, or claude /logout then say "No" to 
    the API key approval before login.
    · Trying to use ANTHROPIC_API_KEY? claude /logout to sign out of claude.ai.
                                                                                                                    
❯ /model gemini/gemini-2.0-flash                                                                                    
  ⎿  API error: 500 {"type":"error","error":{"type":"api_error","message":"failed to get config for provider: not   
     found"}}

## Claude (Message 22)

두 문제를 한번에 해결합니다.

---

## 1단계 — 기존 환경 완전 정리

```bash
# Bifrost 전체 종료
pkill -f bifrost
pkill -f maximhq

# 혹시 zshrc에 ANTHROPIC_API_KEY 설정되어 있으면 확인
grep -n "ANTHROPIC_API_KEY" ~/.zshrc
```

ANTHROPIC_API_KEY가 있으면 해당 줄 삭제합니다.

---

## 2단계 — Bifrost 직접 실행 테스트

터미널을 **두 개** 엽니다.

**터미널 1:**
```bash
# 실제 키로 직접 실행 (백그라운드 아님)
GEMINI_API_KEY="AIza실제키여기" npx -y @maximhq/bifrost
```

실행 후 로그가 흘러야 합니다. 멈추지 말고 그대로 두세요.

**터미널 2:**
```bash
# Claude Pro 토큰만 사용 (ANTHROPIC_API_KEY 없이)
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
```

---

## 3단계 — 세션 안에서 모델 전환

```bash
/model gemini/gemini-2.0-flash
```

---

## 터미널 1 로그에서 에러 확인

Bifrost가 `exit 1`로 죽는 원인이 로그에 찍힙니다.

```bash
cat /tmp/bifrost.log
```

내용 보내주시면 바로 해결합니다.

## Human (Message 23)

Last login: Sun Apr  5 17:10:05 on ttys001
(base) dkmac@MacBook-Air-3 ~ % claude-gemini
[3] 66047
🚀 Bifrost starting...
[3]  + exit 1     GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost > /tmp/bifrost.log 2>&
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · API Usage Billing
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                    
                                                                                                                    
 ⚠Auth conflict: Both a token (claude.ai) and an API key (ANTHROPIC_API_KEY) are set. This may lead to unexpected   
  behavior.                                                                                                         
    · Trying to use claude.ai? Unset the ANTHROPIC_API_KEY environment variable, or claude /logout then say "No" to 
    the API key approval before login.
    · Trying to use ANTHROPIC_API_KEY? claude /logout to sign out of claude.ai.
                                                                                                                    
❯ /model gemini/gemini-2.0-flash                                                                                    
  ⎿  API error: 500 {"type":"error","error":{"type":"api_error","message":"failed to get config for provider: not   
     found"}}

## Claude (Message 24)

로그에서 원인이 명확합니다.

```
config file not found at path: /Users/dkmac/.config/bifrost/config.json
```

Bifrost가 Gemini API 키를 환경변수가 아닌 **config.json 파일**에서 읽는 구조입니다.

---

## 해결 — config.json 생성

터미널 1 (Bifrost 실행 중인 창) `Ctrl+C` 로 종료 후:

```bash
# 디렉토리 생성
mkdir -p ~/.config/bifrost

# config.json 작성 (실제 키 그대로 사용)
cat > ~/.config/bifrost/config.json << 'EOF'
{
  "providers": {
    "gemini": {
      "api_key": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"
    }
  }
}
EOF
```

---

## Bifrost 재시작

**터미널 1:**
```bash
npx -y @maximhq/bifrost
```

로그에서 아래가 보이면 성공:
```
successfully started bifrost, serving UI on http://localhost:8080
```

**터미널 2:**
```bash
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
```

그리고 세션 안에서:
```bash
/model gemini/gemini-2.0-flash
```

---

## Auth 충돌 경고도 동시에 해결

zshrc에 `ANTHROPIC_API_KEY`가 설정되어 있는지 확인:

```bash
grep "ANTHROPIC_API_KEY" ~/.zshrc
```

있으면 해당 줄 삭제 후 `source ~/.zshrc`.

## Human (Message 25)

(base) dkmac@MacBook-Air-3 ~ % ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                    
❯ /model gemini/gemini-2.0-flash                                                                                    
  ⎿  API error: 500 {"type":"error","error":{"type":"api_error","message":"no keys found for provider: gemini and   
     model: gemini-2.0-flash"}}

## Claude (Message 26)

거의 다 왔습니다. 에러 메시지가 바뀌었습니다.

```
no keys found for provider: gemini
```

config.json은 읽혔는데 키 형식이 맞지 않는 것입니다.

---

## config.json 형식 수정

```bash
cat > ~/.config/bifrost/config.json << 'EOF'
{
  "providers": [
    {
      "provider_key": "gemini",
      "api_key": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"
    }
  ]
}
EOF
```

---

## Bifrost 재시작

터미널 1에서 `Ctrl+C` 후:

```bash
npx -y @maximhq/bifrost
```

---

## 터미널 2에서 다시 연결

```bash
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
```

세션 안에서:

```bash
/model gemini/gemini-2.0-flash
```

결과 알려주세요.

## Human (Message 27)

*(내용 없음)*

## Claude (Message 28)

에러 메시지에 정확한 형식이 나와 있습니다.

```
got array, want object
```

providers가 배열`[]`이 아닌 객체`{}`여야 합니다.

---

## config.json 올바른 형식으로 재작성

```bash
cat > ~/.config/bifrost/config.json << 'EOF'
{
  "$schema": "https://www.getbifrost.ai/schema",
  "providers": {
    "gemini": {
      "api_key": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"
    }
  }
}
EOF
```

---

## Bifrost 재시작

```bash
npx -y @maximhq/bifrost
```

이번엔 schema 경고 없이 정상 시작될 것입니다.

---

## 터미널 2에서 연결

```bash
ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
```

```bash
/model gemini/gemini-2.0-flash
```

## Human (Message 29)

*(내용 없음)*

## Claude (Message 30)

에러에서 정확한 스키마가 보입니다.

```
missing property 'keys'
additional properties 'api_key' not allowed
```

`api_key`가 아니라 `keys` 배열 형식입니다.

---

## config.json 최종 수정

```bash
cat > ~/.config/bifrost/config.json << 'EOF'
{
  "$schema": "https://www.getbifrost.ai/schema",
  "providers": {
    "gemini": {
      "keys": [
        {
          "value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"
        }
      ]
    }
  }
}
EOF
```

---

## 포트 충돌도 해결 (8080 이미 사용 중)

```bash
# 기존 Bifrost 완전 종료
pkill -f bifrost
lsof -ti:8080 | xargs kill -9
```

---

## Bifrost 재시작

```bash
npx -y @maximhq/bifrost
```

이번엔 `no keys found` 에러 없이 정상 시작될 것입니다.

## Human (Message 31)

(base) dkmac@MacBook-Air-3 ~ % ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac
                                                                                                                     
❯ /model gemini/gemini-2.0-flash                                                                                     
  ⎿  Model 'gemini/gemini-2.0-flash' not found

## Claude (Message 32)

모델명 형식 문제입니다. Bifrost에서 사용하는 실제 모델 ID를 확인해야 합니다.

---

## Bifrost UI에서 모델명 확인

브라우저에서 [http://localhost:8080](http://localhost:8080) 접속 → Models 또는 Providers 메뉴에서 Gemini 모델 목록 확인

---

## 또는 아래 모델명들 순서대로 시도

```bash
/model gemini-2.0-flash
```

안되면:

```bash
/model google/gemini-2.0-flash
```

안되면:

```bash
/model gemini-2.0-flash-001
```

---

## 또는 Bifrost API로 직접 모델 목록 조회

새 터미널에서:

```bash
curl http://localhost:8080/anthropic/v1/models | python3 -m json.tool
```

여기서 나오는 `id` 값이 실제 사용 가능한 모델명입니다. 결과 보내주세요.

## Human (Message 33)

no keys found for provider: gemini and model: gemini-2.0-flash

## Claude (Message 34)

Bifrost가 config.json의 키를 여전히 못 읽고 있습니다. UI에서 직접 등록하는 방법으로 전환합니다.

---

## Bifrost UI에서 직접 API Key 등록

브라우저에서 [http://localhost:8080](http://localhost:8080) 접속

**Providers** 또는 **Settings** 메뉴 → **Gemini** → API Key 입력란에 키 붙여넣기 → **Save**

---

## UI가 없으면 API로 직접 등록

```bash
curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": ["AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"]
  }'
```

---

## 그래도 안되면 — LiteLLM으로 전환 (더 안정적)

Bifrost가 계속 말썽이면 LiteLLM 프록시가 훨씬 안정적입니다.

```bash
# 설치
pip install litellm

# 실행 (한 줄)
GEMINI_API_KEY="AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34" \
litellm --model gemini/gemini-2.0-flash --port 8080
```

새 터미널에서:

```bash
ANTHROPIC_BASE_URL=http://localhost:8080 claude
```

세션 안에서:

```bash
/model gemini/gemini-2.0-flash
```

LiteLLM 방식 시도해볼까요?

## Human (Message 35)

Last login: Sun Apr  5 17:21:34 on ttys009
(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": ["AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"]
  }'
{"is_bifrost_error":false,"status_code":400,"error":{"message":"Invalid JSON: json: cannot unmarshal string into Go struct field .keys of type schemas.Key"},"extra_fields":{}}

## Claude (Message 36)

Key도 객체 형식이어야 합니다.

```bash
curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": [{"value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"}]
  }'
```

결과 알려주세요. 성공하면 `{"success": true}` 같은 응답이 옵니다.

## Human (Message 37)

(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": ["AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"]
  }'
{"is_bifrost_error":false,"status_code":400,"error":{"message":"Invalid JSON: json: cannot unmarshal string into Go struct field .keys of type schemas.Key"},"extra_fields":{}}
(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": [{"value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"}]
  }'
{"is_bifrost_error":false,"status_code":409,"error":{"message":"Provider gemini already exists"},"extra_fields":{}}

## Claude (Message 38)

`409 Already exists` — provider는 이미 등록됐습니다. 키만 따로 추가하면 됩니다.

```bash
curl -X POST http://localhost:8080/api/providers/gemini/keys \
  -H "Content-Type: application/json" \
  -d '{"value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"}'
```

결과 알려주세요.

## Human (Message 39)

(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": ["AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"]
  }'
{"is_bifrost_error":false,"status_code":400,"error":{"message":"Invalid JSON: json: cannot unmarshal string into Go struct field .keys of type schemas.Key"},"extra_fields":{}}
(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "gemini",
    "keys": [{"value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"}]
  }'
{"is_bifrost_error":false,"status_code":409,"error":{"message":"Provider gemini already exists"},"extra_fields":{}}
(base) dkmac@MacBook-Air-3 ~ % curl -X POST http://localhost:8080/api/providers/gemini/keys \
  -H "Content-Type: application/json" \
  -d '{"value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"}'
Method Not Allowed%

## Claude (Message 40)

API 경로가 다릅니다. Bifrost UI에서 직접 처리하는 게 가장 빠릅니다.

---

## 브라우저에서 직접 등록

**[http://localhost:8080](http://localhost:8080)** 접속 → 화면 캡처해서 보내주세요.

UI 구조를 보면 정확한 메뉴 경로를 안내할 수 있습니다.

---

## 또는 Bifrost 완전 초기화 후 재시작

```bash
# Bifrost 종료
pkill -f bifrost
lsof -ti:8080 | xargs kill -9

# DB 초기화 (기존 잘못된 설정 제거)
rm -rf ~/.config/bifrost/
rm -rf ~/Library/Caches/bifrost/

# config 새로 작성
mkdir -p ~/.config/bifrost
cat > ~/.config/bifrost/config.json << 'EOF'
{
  "$schema": "https://www.getbifrost.ai/schema",
  "providers": {
    "gemini": {
      "keys": [
        {
          "value": "AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"
        }
      ]
    }
  }
}
EOF

# 재시작
npx -y @maximhq/bifrost
```

---

## 그래도 안되면 LiteLLM으로 전환

Bifrost가 계속 문제라면 LiteLLM이 훨씬 단순합니다.

```bash
pip install 'litellm[proxy]'

GEMINI_API_KEY="AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34" \
litellm --model gemini/gemini-2.0-flash --port 4000
```

새 터미널:

```bash
ANTHROPIC_BASE_URL=http://localhost:4000 claude
```

**LiteLLM 방식으로 전환할까요?** Bifrost보다 설정이 훨씬 단순합니다.

## Human (Message 41)

▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · API Usage Billing
  ▘▘ ▝▝    /Users/dkmac


 Auth conflict: Both a token (claude.ai) and an API key (ANTHROPIC_API_KEY) are set. This may lead to unexpected
 behavior.
    · Trying to use claude.ai? Unset the ANTHROPIC_API_KEY environment variable, or claude /logout then say "No" to
    the API key approval before login.
    · Trying to use ANTHROPIC_API_KEY? claude /logout to sign out of claude.ai.
                                                                                                                    
❯ /model gemini/gemini-2.0-flash                                                                                    
  ⎿  Model 'gemini/gemini-2.0-flash' not found

## Claude (Message 42)

두 가지 문제를 동시에 해결합니다.

---

## 문제 1 — ANTHROPIC_API_KEY 충돌 제거

```bash
# zshrc에서 확인
grep -n "ANTHROPIC_API_KEY" ~/.zshrc
```

있으면:
```bash
nano ~/.zshrc
# ANTHROPIC_API_KEY 있는 줄 삭제
# Ctrl+O → Enter → Ctrl+X
source ~/.zshrc
```

현재 세션에서 즉시 제거:
```bash
unset ANTHROPIC_API_KEY
```

---

## 문제 2 — Bifrost 포기, LiteLLM으로 전환

Bifrost가 계속 모델을 못 찾는 문제가 반복되고 있습니다. LiteLLM이 훨씬 안정적입니다.

**터미널 1 — Bifrost 종료 후 LiteLLM 설치 및 실행:**

```bash
# Bifrost 종료
pkill -f bifrost
lsof -ti:8080 | xargs kill -9

# LiteLLM 설치
pip install 'litellm[proxy]'

# 실행
GEMINI_API_KEY="AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34" \
litellm --model gemini/gemini-2.0-flash --port 4000
```

**터미널 2 — Claude 연결:**

```bash
unset ANTHROPIC_API_KEY
ANTHROPIC_BASE_URL=http://localhost:4000 claude
```

세션 안에서:
```bash
/model gemini/gemini-2.0-flash
```

---

## zshrc도 LiteLLM 기준으로 업데이트

```bash
nano ~/.zshrc
```

기존 bifrost 관련 전체 삭제 후 아래로 교체:

```bash
# ─── Claude Code + Gemini (LiteLLM) ──────────────────────
export GEMINI_API_KEY="AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34"

litellm-start() {
  if pgrep -f "litellm" > /dev/null; then
    echo "✅ LiteLLM already running"
  else
    GEMINI_API_KEY=$GEMINI_API_KEY litellm \
      --model gemini/gemini-2.0-flash \
      --port 4000 > /tmp/litellm.log 2>&1 &
    echo "🚀 LiteLLM starting..."
    sleep 4
  fi
}

alias claude-gemini='litellm-start && ANTHROPIC_BASE_URL=http://localhost:4000 claude'
alias claude-normal='ANTHROPIC_BASE_URL="" claude'
alias litellm-stop='pkill -f litellm && echo "🛑 LiteLLM stopped"'
alias litellm-log='tail -f /tmp/litellm.log'
# ──────────────────────────────────────────────────────────
```

```bash
source ~/.zshrc
```

## Human (Message 43)

Last login: Sun Apr  5 17:29:35 on ttys009
(base) dkmac@MacBook-Air-3 ~ % claude
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac

❯ ok?                                                                           
  ⎿  Please run /login · API Error: 401 
     {"type":"error","error":{"type":"authentication_error","message":"Invalid  
     authentication credentials"},"request_id":"req_011CZkK4go3MpeMyWkDf3CGb"}

## Claude (Message 44)

`ANTHROPIC_BASE_URL`이 아직 LiteLLM 주소를 가리키고 있습니다. LiteLLM이 꺼진 상태에서 claude를 실행하면 이 에러가 납니다.

---

## 즉시 해결

```bash
# BASE_URL 초기화
unset ANTHROPIC_BASE_URL
unset ANTHROPIC_API_KEY

# 일반 Claude 실행
claude
```

---

## 근본 원인 — zshrc 확인

```bash
grep -n "ANTHROPIC" ~/.zshrc
```

`ANTHROPIC_BASE_URL`이 zshrc에 하드코딩되어 있으면 매번 충돌납니다. 있으면 삭제해야 합니다.

```bash
nano ~/.zshrc
# ANTHROPIC_BASE_URL 있는 줄 삭제
# Ctrl+O → Enter → Ctrl+X
source ~/.zshrc
```

---

## 정리된 최종 구조

| 명령어 | 동작 |
|--------|------|
| `claude` 또는 `claude-normal` | Claude Pro 계정 토큰으로 정상 실행 |
| `claude-gemini` | LiteLLM 시작 후 Gemini 라우팅 |

`claude-normal`은 항상 `ANTHROPIC_BASE_URL`을 unset하고 실행하도록 alias가 잡혀 있어야 합니다.

## Human (Message 45)

(base) dkmac@MacBook-Air-3 ~ % claude
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac

❯ ok?                                                                           
  ⎿  Please run /login · API Error: 401 
     {"type":"error","error":{"type":"authentication_error","message":"Invalid  
     authentication credentials"},"request_id":"req_011CZkK85nyugQStoDmmrHQv"}

## Claude (Message 46)

unset 후에도 에러가 난다면 zshrc에 문제가 있습니다.

---

## 현재 환경변수 전체 확인

```bash
env | grep -E "ANTHROPIC|GEMINI|OPENAI"
```

결과 보내주세요. 어디서 오는 변수인지 바로 찾겠습니다.

## Human (Message 47)

(base) dkmac@MacBook-Air-3 ~ % env | grep -E "ANTHROPIC|GEMINI|OPENAI"
GEMINI_API_KEY=AIzaSyCTlQhikRrYqgJG_YjgFY3QDlvNld0xb34

## Claude (Message 48)

환경변수는 깨끗합니다. 문제는 다른 곳에 있습니다.

---

## Claude Code 인증 토큰 재설정

```bash
# 로그아웃
claude logout

# 다시 로그인
claude login
```

브라우저가 열리면 claude.ai 계정으로 로그인 → 완료 후 다시 시도:

```bash
claude
```

## Human (Message 49)

(base) dkmac@MacBook-Air-3 ~ % claude login
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac

❯ ok??                                                                          
  ⎿  Please run /login · API Error: 401 
     {"type":"error","error":{"type":"authentication_error","message":"Invalid  
     authentication credentials"},"request_id":"req_011CZkKJRbddeSAUixKqKRSm"}
                                                                                
❯ login                                                                         
  ⎿  Please run /login · API Error: 401                                         
     {"type":"error","error":{"type":"authentication_error","message":"Invalid 
     authentication credentials"},"request_id":"req_011CZkKJUp7UgsWVBHyucoUj"}

## Claude (Message 50)

Claude Code 세션 안에서 명령어를 입력하고 있습니다. 터미널 프롬프트(`%`)에서 실행해야 합니다.

---

## 먼저 Claude Code 세션 종료

```
/exit
```

또는 `Ctrl+C`

---

## 터미널 프롬프트에서 실행

```bash
claude logout
```

```bash
claude login
```

`(base) dkmac@MacBook-Air-3 ~ %` 이 프롬프트에서 입력하는 것이 맞습니다.

## Human (Message 51)

❯ %                                                                             (base) dkmac@MacBook-Air-3 ~ % claude login
▗ ▗   ▖ ▖  Claude Code v2.1.92
           Opus 4.6 with medium effort · Claude Pro
  ▘▘ ▝▝    /Users/dkmac

❯ ok?                                                                           
  ⎿  Please run /login · API Error: 401 
     {"type":"error","error":{"type":"authentication_error","message":"Invalid  
     authentication credentials"},"request_id":"req_011CZkKPhFMvujtQyc2L5aFm"}
                                                                                
❯ login                                                                         
  ⎿  Please run /login · API Error: 401                                         
     {"type":"error","error":{"type":"authentication_error","message":"Invalid 
     authentication credentials"},"request_id":"req_011CZkKPoBJT6AUWqXCixj65"}

## Claude (Message 52)

`claude login`을 실행하면 Claude Code 세션이 열리면서 프롬프트(`❯`)가 나오고 있습니다. 이 상태에서는 `/login` 슬래시 명령어를 써야 합니다.

---

## 지금 바로

Claude Code 프롬프트(`❯`) 에서:

```
/login
```

브라우저가 열리면 claude.ai 로그인 완료 후 돌아오면 됩니다.

---

*Archived from: `f2b906d8-d111-46d2-acdf-da7a7d0d01e2.json`*