---
title: Claude Cowork 기능과 Claude Code 구현 방법
date: 2026-01-15
time: 09:13
uuid: ef5f1923-8580-477b-be45-384789e9cb3f
type: conversation
tags: [archive, conversation]
---

# Claude Cowork 기능과 Claude Code 구현 방법

**날짜**: 2026-01-15 09:13

---

## Human (Message 1)

claude에서 새로 발표한 co-work에 대해 설명하고 클로드 코드를 통해서 기능구현하는 방법을 설명해줘

## Claude (Message 2)

사용자가 Claude의 새로운 "co-work" 기능에 대해 물어보고 있습니다. 그리고 Claude Code를 통해 기능을 구현하는 방법을 알고 싶어합니다.

먼저, 저는 2025년 1월까지의 정보를 알고 있습니다. "co-work"라는 기능에 대해서는 제 지식 컷오프 이후에 발표되었을 가능성이 있습니다. 현재 날짜가 2025년 1월 15일이므로, 최근에 발표된 기능일 수 있습니다.

제 시스템 프롬프트에 따르면:

- Claude Code는 명령줄 도구로 개발자가 터미널에서 직접 Claude에게 코딩 작업을 위임할 수 있는 도구입니다.
- 최근 모델은 Claude Sonnet 4.5와 Claude Haiku 4.5입니다.

"co-work"에 대해 정확한 정보를 제공하려면 웹 검색을 해야 합니다. 제 지식 컷오프 이후의 기능이므로 검색이 필요합니다.Claude의 "co-work" 기능에 대해 정확한 최신 정보를 찾아보겠습니다.

```
This block is not supported on your current device yet.
```

좋아요, 이제 Claude Cowork에 대한 정보를 충분히 수집했습니다. 주요 내용을 정리하면:

1. Cowork는 2026년 1월 13일경에 발표된 신제품
2. Claude Code의 non-technical 버전
3. macOS Desktop 앱에서만 사용 가능 (현재)
4. Max 구독자만 사용 가능 ($100-200/month)
5. 파일 시스템에 접근하여 읽기/쓰기/생성 가능
6. 약 10일 만에 Claude Code를 사용하여 개발됨
7. 샌드박스 환경에서 작동

이제 사용자가 요청한 대로:

1. Cowork 설명
2. Claude Code를 통한 기능 구현 방법

사용자의 프로필을 보면:

- HR 전문가이면서 Full-stack developer
- 자동화와 생산성 도구에 관심
- Notion, Python 등 다양한 도구 활용
- 한국어로 소통 선호

한국어로 명확하고 실용적인 설명을 제공하겠습니다.# 🎯 Claude Cowork 완벽 가이드

## 📌 Cowork란?

Cowork는 Anthropic이 2026년 1월에 출시한 Claude Code의 non-developer 버전입니다. 개발자들이 Claude Code를 코딩뿐만 아니라 휴가 계획, 슬라이드 제작, 이메일 정리, 구독 취소, 결혼 사진 복구 등 다양한 작업에 활용하는 것을 보고 만들어졌습니다.

### 핵심 특징

**1. 파일 시스템 접근**

- 지정한 폴더의 파일을 읽고, 편집하고, 생성 가능
- 샌드박스 컨테이너 환경에서 작동하여 보안 유지

**2. 자율적 실행**

- 작업을 큐에 추가하면 Claude가 자율적으로 완료
- 일반 대화보다 훨씬 높은 자율성으로 작업 수행

**3. 연결된 도구 활용**

- Gmail, Google Drive 등 기존 커넥터와 통합
- Chrome 확장과 결합하면 웹 브라우저 제어 가능

### 사용 가능 대상

- **현재**: macOS Desktop 앱의 Claude Max 구독자 전용
- **향후**: Windows 지원 및 다른 플랜으로 확대 예정

---

## 💡 실제 활용 사례

CSP님의 업무 맥락에서 Cowork를 활용할 수 있는 시나리오:

```
1. 리더십 평가 보고서 자동 생성
   - 흩어진 피드백 노트 → 구조화된 보고서

2. HR 데이터 분석 자동화
   - 다운로드 폴더의 엑셀 파일들 → 종합 분석 대시보드

3. 교육 자료 일괄 처리
   - 스크린샷 모음 → 정리된 PPT 슬라이드

4. 파일 정리 자동화
   - 다운로드 폴더 → 프로젝트별 분류 및 이름 변경
```

---

## 🔧 Claude Code로 Cowork 같은 기능 구현하기

Cowork가 10일 만에 Claude Code 자체를 사용해서 개발되었다는 점을 활용하면, Claude Code를 통해 유사한 자동화 워크플로우를 만들 수 있습니다.

### 방법 1: Claude Code 터미널 활용

```bash
# Claude Code 설치 (미설치 시)
pip install anthropic-claude-code

# 기본 사용법
claude "downloads 폴더의 영수증 스크린샷들을 분석해서 지출 내역 엑셀 만들어줘"

# 특정 폴더 지정
cd ~/Documents/HR-Reports
claude "이 폴더의 모든 피드백 파일을 읽고 통합 보고서를 만들어줘"
```

### 방법 2: Python 스크립트로 자동화

```python
# hr_automation.py
from anthropic import Anthropic
import os
from pathlib import Path

client = Anthropic(api_key=os.environ.get("ANTHROPIC_API_KEY"))

def automate_hr_task(folder_path, instruction):
    """
    Claude Code 방식의 자동화 함수
    """
    # 폴더 내 파일 목록 수집
    files = list(Path(folder_path).rglob("*.*"))
    context = "\n".join([f"- {f.name}" for f in files[:20]])

    message = client.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=4096,
        messages=[{
            "role": "user",
            "content": f"""
작업 폴더: {folder_path}
파일 목록:
{context}

작업 지시: {instruction}

파일을 읽고, 분석하고, 필요한 작업을 수행하는 Python 코드를 작성해줘.
코드는 즉시 실행 가능해야 하며, 결과물을 저장해야 해.
"""
        }]
    )

    return message.content

# 사용 예시
result = automate_hr_task(
    folder_path="~/Downloads/evaluation-feedback",
    instruction="모든 피드백을 통합하여 리더십 평가 종합 보고서를 만들어줘"
)
```

### 방법 3: MCP 서버와 통합

```python
# mcp_cowork_agent.py
"""
Notion MCP와 연동한 Cowork 스타일 에이전트
"""

from anthropic import Anthropic
import json

def notion_cowork_agent(task_description, folder_path):
    """
    로컬 파일 작업 + Notion 저장 자동화
    """
    client = Anthropic()

    # 1단계: 로컬 파일 분석 및 작업
    analysis = client.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=8000,
        messages=[{
            "role": "user",
            "content": f"""
폴더: {folder_path}
작업: {task_description}

다음 단계로 실행:
1. 폴더 내 파일 스캔 및 분석
2. 필요한 데이터 추출
3. 보고서 작성
4. Notion에 저장할 구조화된 데이터 생성

결과를 JSON 형식으로 제공:
{{
  "summary": "작업 요약",
  "files_processed": ["파일 목록"],
  "report": "마크다운 형식 보고서",
  "notion_data": {{...}}
}}
"""
        }]
    )

    # 2단계: Notion에 자동 저장
    # (Notion MCP 도구 사용)

    return analysis

# 사용 예시
agent_result = notion_cowork_agent(
    task_description="리더십 워크숍 피드백을 분석하고 개선점을 도출하여 Notion에 저장",
    folder_path="~/HR/workshop-feedback"
)
```

### 방법 4: Skills 활용 (가장 권장)

CSP님의 Notion Skills을 활용한 방법:

```python
# claude_skill_automation.py
"""
기존 Notion Skills를 활용한 Cowork 스타일 워크플로우
"""

def skill_based_cowork(task_type):
    """
    각 작업 타입에 맞는 Skill 자동 호출
    """
    skill_mapping = {
        "knowledge_capture": "/mnt/skills/user/notion-knowledge-capture/SKILL.md",
        "research": "/mnt/skills/user/notion-research-documentation/SKILL.md",
        "document": "/mnt/skills/public/docx/SKILL.md",
        "presentation": "/mnt/skills/public/pptx/SKILL.md"
    }

    # Claude에게 직접 요청
    prompt = f"""
{task_type}에 해당하는 작업을 수행해줘.

관련 Skill 참조: {skill_mapping.get(task_type)}

작업 순서:
1. Skill 문서 읽기
2. 베스트 프랙티스 적용
3. 결과물 생성
4. Notion에 저장
"""

    return prompt

# 터미널에서 실행
# claude "$(python -c "from claude_skill_automation import skill_based_cowork; print(skill_based_cowork('knowledge_capture'))")"
```

---

## ⚡ 실전 워크플로우: HR 리더십 평가 자동화

```python
# leadership_evaluation_cowork.py
"""
Cowork 스타일의 완전 자동화 HR 워크플로우
"""

import anthropic
import os
from pathlib import Path
from datetime import datetime

class LeadershipEvaluationAgent:
    def __init__(self):
        self.client = anthropic.Anthropic(
            api_key=os.environ.get("ANTHROPIC_API_KEY")
        )
        self.workspace = Path("~/HR/evaluations").expanduser()

    def run_cowork_flow(self, quarter):
        """
        Cowork처럼 자율적으로 작업 수행
        """
        tasks = [
            self.collect_feedback_files(quarter),
            self.analyze_feedback(),
            self.generate_report(),
            self.create_presentation(),
            self.save_to_notion(),
            self.send_summary_email()
        ]

        results = []
        for task in tasks:
            print(f"🔄 실행 중: {task.__name__}")
            result = task()
            results.append(result)
            print(f"✅ 완료: {task.__name__}")

        return results

    def collect_feedback_files(self, quarter):
        """1단계: 피드백 파일 수집"""
        feedback_files = list(self.workspace.glob(f"*{quarter}*.xlsx"))
        return {
            "collected": len(feedback_files),
            "files": [f.name for f in feedback_files]
        }

    def analyze_feedback(self):
        """2단계: AI 분석"""
        response = self.client.messages.create(
            model="claude-sonnet-4-5-20250929",
            max_tokens=8000,
            messages=[{
                "role": "user",
                "content": """
workspace 폴더의 모든 피드백 데이터를 분석:

1. 공통 패턴 추출
2. 강점/개선점 분류
3. 정량 지표 계산
4. 액션 아이템 도출

구조화된 JSON으로 결과 제공
"""
            }]
        )
        return response.content

    def generate_report(self):
        """3단계: BCG 스타일 보고서 생성"""
        # docx skill 활용
        pass

    def create_presentation(self):
        """4단계: PPT 생성"""
        # pptx skill 활용
        pass

    def save_to_notion(self):
        """5단계: Notion 저장"""
        # notion-knowledge-capture skill 활용
        pass

    def send_summary_email(self):
        """6단계: 이메일 발송"""
        # Gmail connector 활용
        pass

# 실행
if __name__ == "__main__":
    agent = LeadershipEvaluationAgent()
    results = agent.run_cowork_flow(quarter="2025Q1")
    print(f"\n🎉 전체 워크플로우 완료: {datetime.now()}")
```

---

## 🎓 핵심 인사이트

### Cowork vs Claude Code

- **Cowork**: GUI 기반, 비개발자용, 샌드박스 폴더
- **Claude Code**: 터미널 기반, 개발자용, 전체 시스템 접근 가능

### CSP님께 추천하는 접근법

1. **단기 (현재)**
   - Claude Code 터미널로 HR 자동화 실험
   - 기존 Notion Skills와 통합

2. **중기 (Cowork 접근 가능 시)**
   - Max 구독 후 Cowork 도입
   - 리더십 평가 워크플로우 완전 자동화

3. **장기 (확장)**
   - Custom MCP 서버 개발
   - 조직 전체 DX 교육 자료로 활용

---

## ⚠️ 보안 주의사항

Cowork는 prompt injection 공격에 취약할 수 있으며, 파일 삭제 등 잠재적으로 파괴적인 작업을 수행할 수 있습니다.

**안전하게 사용하는 방법:**

- 중요한 파일은 백업 후 작업
- 명확하고 구체적인 지시사항 제공
- 신뢰할 수 있는 소스의 파일만 처리
- 처음에는 테스트 폴더로 시작

---

도움이 되셨나요? 구체적으로 어떤 HR 워크플로우를 자동화하고 싶으신지 말씀해주시면 맞춤형 코드를 작성해드리겠습니다! 🚀

## Human (Message 3)

잘 모르겠다.. 쩝.. 내가 클로드 코드 사용법을 잘 몰라서 그런걸까?

## Claude (Message 4)

사용자가 Claude Code 사용법을 잘 모르겠다고 하네요. 제가 너무 기술적으로 설명했나 봅니다.

사용자는:

- 17년 HR 경력
- Vibe coder (실용적 자동화 전문)
- Python, 데이터 분석 가능
- Notion Skills 이미 활용 중

하지만 Claude Code의 실제 사용법이 명확하지 않은 것 같습니다.

더 실용적이고 단계별로, 그리고 실제 화면/명령어 중심으로 설명해야 할 것 같습니다.

1. Claude Code가 정확히 뭔지
2. 어떻게 설치하는지
3. 가장 간단한 사용 예시부터
4. CSP님의 실제 업무에 바로 적용 가능한 예시

이렇게 다시 접근해보겠습니다.아! 제가 너무 복잡하게 설명했네요 😅

실용적으로 다시 설명드릴게요. **Claude Code는 사실 엄청 간단**합니다!

---

## 🎯 Claude Code, 진짜 간단하게

### 1️⃣ Claude Code가 뭐냐면...

**터미널에서 Claude한테 "이것좀 해줘" 하면 알아서 다 해주는 도구**입니다.

```bash
# 이게 전부입니다
claude "이 폴더의 엑셀 파일들 합쳐서 요약본 만들어줘"
```

그러면 Claude가:

- 파일 찾고
- 읽고
- 분석하고
- 새 파일 만들고
- 저장까지 **알아서** 다 해줍니다

---

## 🚀 5분 만에 시작하기

### Step 1: 설치 (1분)

```bash
# 터미널에서 이것만 실행
pip install claude-code

# API 키 설정 (Anthropic 콘솔에서 발급)
export ANTHROPIC_API_KEY='your-api-key'
```

### Step 2: 첫 번째 명령 (1분)

```bash
# 다운로드 폴더로 이동
cd ~/Downloads

# Claude한테 부탁
claude "여기 파일들 이름 정리해줘. 날짜_파일명 형식으로"
```

**끝!** Claude가 알아서 파일 이름 다 바꿔줍니다.

---

## 💼 CSP님 업무에 바로 쓸 수 있는 예시

### 예시 1: 피드백 파일 정리

```bash
cd ~/Documents/리더십평가/피드백

claude "
이 폴더에 있는 모든 워드/PDF 파일을 읽고:
1. 각 파일의 핵심 피드백 3줄로 요약
2. 긍정/부정/개선점으로 분류
3. summary.xlsx 파일로 저장해줘
"
```

### 예시 2: 교육자료 만들기

```bash
cd ~/Desktop/DX교육

claude "
'AI활용법.pptx' 파일 만들어줘:
- 슬라이드 5장
- 주제: HR 담당자를 위한 Claude 활용법
- 실용적인 예시 중심으로
"
```

### 예시 3: 데이터 분석

```bash
cd ~/HR데이터

claude "
2024년_인력현황.xlsx 파일 분석해서:
- 부서별 인원 현황 그래프
- 연령대 분포 파이차트
- 주요 인사이트 3가지
보고서.html로 만들어줘
"
```

---

## 🤔 Cowork vs Claude Code 차이

**Claude Code** (지금 바로 쓸 수 있음)

```bash
# 터미널에서
claude "파일 정리해줘"
```

**Cowork** (macOS 앱에서만, Max 구독 필요)

```
GUI에서 폴더 선택 → 대화로 지시
(터미널 필요 없음)
```

**결론**: 지금은 **Claude Code**가 더 실용적입니다! CSP님은 Python 할 줄 아시니까 터미널도 익숙하실 거예요.

---

## 🎬 지금 바로 해볼 수 있는 것

### 1번 미션: 파일 정리

```bash
# 1. 테스트 폴더 만들기
mkdir ~/Desktop/claude_test
cd ~/Desktop/claude_test

# 2. 테스트 파일 만들기
touch "제목없음1.txt" "asdf.txt" "123.txt"

# 3. Claude한테 부탁
claude "이 파일들 의미있는 이름으로 바꿔줘"
```

### 2번 미션: 간단한 분석

```bash
# Downloads 폴더에 엑셀 파일 아무거나 하나 두고
cd ~/Downloads

claude "
가장 최근 엑셀 파일 하나 열어서:
- 총 몇 개 행인지
- 주요 컬럼이 뭔지
- 간단한 통계
알려줘
"
```

---

## 🚨 자주 하는 실수

❌ **복잡하게 지시**: "파이썬으로 pandas 써서 데이터프레임 만들고..."
✅ **간단하게 지시**: "엑셀 파일 합쳐줘"

❌ **코드 짜달라고**: "이거 하는 코드 작성해줘"  
✅ **결과 달라고**: "이거 해줘"

Claude Code는 **코드 짜는 도구가 아니라 일 시키는 도구**입니다!

---

## 💡 핵심 요약

**Claude Code = 터미널에서 쓰는 AI 비서**

```bash
# 이 한 줄이면 됩니다
claude "내가 원하는 작업"
```

- ✅ 파일 읽고 쓰기
- ✅ 데이터 분석
- ✅ 문서 만들기
- ✅ 자동화

**Cowork = GUI 버전** (나중에 나오면 더 쉬움)

---

이제 좀 감이 오시나요?

**한 줄 요약**: Claude Code는 그냥 "claude '이거해줘'" 하면 끝입니다 😊

혹시 설치부터 막히시면 말씀해주세요. 하나씩 같이 해보죠!

---

_Archived from: `ef5f1923-8580-477b-be45-384789e9cb3f.json`_
